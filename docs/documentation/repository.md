# Репозиторий и протокол задач

В этом репозитории лежат:

- примеры задач в `docs/tasks/` (`TASK-*.md`);
- фрагменты `.cursor/` (правило frontmatter, skill протокола).

Это образец процесса ведения задач вместе с Cursor, а не функциональность расширения после установки из Marketplace.

Протокол: skill [`.cursor/skills/task-management-protocol/SKILL.md`](../../.cursor/skills/task-management-protocol/SKILL.md).

[← Оглавление](./README.md)
